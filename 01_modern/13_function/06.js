// 함수 선언문
function 함수이름(a, b) {
  return a + b;
}

// 함수 표현식
const 변수명 = function (a, b) {
  return a + b;
};

// 익명 함수 표현식
// const greet = function () {
//   console.log("안녕하세요!");
// };

// greet(); // 안녕하세요!

// 매개변수와 반환값
const add = function (a, b) {
  return a + b;
};

console.log(add(10, 20)); // 30
